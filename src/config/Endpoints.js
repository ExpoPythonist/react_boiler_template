const url = 'http://dev.api.oametrix.io/';
const baseURL = url + 'api/v1/';
export const config = {
  baseURL,
  endpoint: {
    login: 'login/',
    sign_up: 'registration/',
    logout: 'logout/',

  },
}